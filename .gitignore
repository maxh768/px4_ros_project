# Claude Code local settings (machine-specific, must not be committed —
# autoMemoryDirectory is ignored by design if it appears in a checked-in file)
.claude/settings.local.json

# colcon
build/
install/
log/

# fetched via `vcs import src < deps.repos` — pinned, never committed
src/px4_msgs/

# Python
__pycache__/
*.pyc
*.egg-info/

# debug
debug/

# local
.vscode/
.claude/

# flight logs
*.ulg
*.csv
rosbags/
flight_logs/

# local guidelines (not published with the repo)
guidelines/
